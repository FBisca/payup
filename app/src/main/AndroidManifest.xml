<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.payup">

  <application
      android:name=".app.App"
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">

    <activity android:name=".app.home.HomeActivity"/>

    <activity
        android:name=".app.payment.PaymentActivity"
        android:theme="@style/AppTheme.DarkScreen"
        android:windowSoftInputMode="stateAlwaysHidden"/>

    <activity
        android:name=".app.payment.ConfirmationActivity"
        android:theme="@style/AppTheme.DarkScreen"/>

    <activity-alias
        android:name=".Launcher"
        android:targetActivity=".app.home.HomeActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity-alias>

  </application>

</manifest>
